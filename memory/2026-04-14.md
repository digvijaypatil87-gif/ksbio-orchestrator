# Memory — 2026-04-14

## KSBio Analytics Rebuild (Phase 1 Complete)
- FastAPI app running at `localhost:8001` with truth-check passing
- DuckDB loaded: 753 ledgers, 239 debtors, 206 creditors; AR = ₹1,33,45,529.32 from 129 accounts
- Trial balance balanced at ₹58,24,99,554.45
- Streaming XML parser using `lxml.iterparse()` with UTF-16 encoding
- All core ETL + AR + health endpoints verified working

## OpenClaw Bots
- **Main (Jarvis)**: Telegram bot `Ksbio_openclaw_bot` (token: `8505855332:AAFQU80TY70j6UOZ8FTgfhFZv8ahnq-eTK4`), port 18789, config patched for group access
- **HR bot (hiclaw-dashboard)**: Separate instance at port 18790, Telegram bot token `8636782660:AAH3eo6wva7v6HxeZK0pbwakoVBOUNMjjnk`, config updated with groupAllowFrom and requireMention: false
- Both bots updated and restarted to respond in Jarvis group without @mention

## Watchdog Setup
- Watchdog script: `C:\Users\Digvijay Patil\.openclaw\watchdog.ps1` — monitors main (18789) + hr (18790) gateways
- Restart-agent: `C:\Users\Digvijay Patil\.openclaw\restart-agent.js` — Node.js HTTP server on port 19000, auth token `ksbio-restart-2026`
- Watchdog v2 (current): singleton mutex, in-process restart-agent management, 30s check interval
- Both gateway health checks + config integrity monitoring active
- Config golden backups stored at `openclaw.json.golden` and `openclaw.json.golden` (hr profile)

## n8n ETL Workflow (TODO)
- KSBIO Infrastructure Monitor: working, checks 8 services every 5 min
- ERPNext Health Check: working, every hour
- Tally Daily Health Check: broken, points to port 9000 (nothing there)
- KSBio Reconciliation Alert: query stored but never executed
- **ETL pipeline not yet wired** — need to build HTTP Request workflow calling FastAPI endpoints on daily schedule

## Jarvis Group (Telegram)
- Group ID: -1002381931352
- Members: Digvijay, Jarvis (me), HR bot
- All three should now be able to chat without @mention after config updates

## Today's Conversations
- Fixed HR bot group access (was missing groupAllowFrom + requireMention: false)
- Investigated watchdog setup — Claude Code had started building it but never saved/deployed to n8n
- Found restart-agent.js already existed with proper auth + Docker restart logic
- Rebuilt watchdog.ps1 as v2 with singleton mutex + in-process restart-agent management
- Identified ETL workflow gap: pipeline exists in FastAPI but n8n hasn't wired it up yet